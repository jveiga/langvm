# rust language plugin manager

## How to use

### vm init
Installs general plugin list, doesn't overwrite if `data_dir` already exists.

### vm init --force -f
Installs general plugin list, overwrites `data_dir` if it already exists.